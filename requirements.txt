# Product Enrichment Pipeline Dependencies
# For NVIDIA A10 GPU (24GB VRAM)
#
# IMPORTANT: Install torch first with CUDA support:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# Then install the rest:
#   pip install -r requirements.txt

# Core (install separately with CUDA - see above)
# torch>=2.0.0
# torchvision>=0.15.0

# Hugging Face
transformers>=4.35.0
sentence-transformers>=2.2.0
datasets>=2.14.0

# FashionSigLIP dependencies
ftfy>=6.1.0
open_clip_torch>=2.20.0

# Image processing
Pillow>=10.0.0

# Async HTTP
aiohttp>=3.9.0

# Database
asyncpg>=0.29.0

# Environment
python-dotenv>=1.0.0

# Progress bar
tqdm>=4.66.0

# Numerical
numpy>=1.24.0

# Neural translation (Helsinki-NLP MarianMT)
sentencepiece>=0.1.99

# vLLM for fast LLM inference (optional but recommended)
# Install separately: pip install vllm
# vllm>=0.4.0

# Accelerate for efficient model loading
accelerate>=0.25.0
